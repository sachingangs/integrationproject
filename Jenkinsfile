pipeline {
  agent any
  stages {
    stage('') {
      steps {
        build 'build'
      }
    }
  }
  environment {
    DEV = 'vtibappdv01'
    TEST = 'vtibappqa01'
    PROD = 'sbd-tibapp-pd1'
  }
}